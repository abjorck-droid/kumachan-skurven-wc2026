# Session Handoff — 2026-06-12

**Read this first.** Supersedes `00_handoff_2026-06-11.md` (whose evening addendum covers opening-day
firefighting in detail — consult it for the unlock tool, the cron diagnosis, and the bonus-save bug).

## State of the world

Tournament is live, day 2 complete. Both players locked pre-opener (~270 Predictions rows ≈ 135 each,
complete sets). Pipeline is **fully autonomous and verified across two match days**:

> CF Worker cron (15 min) → `workflow_dispatch` → GitHub Actions `scoreboard-poll` →
> poller → resolve_sidegames → scoring_engine → Airtable → live board (60 s edge cache)

Working tree clean; everything through the bonus-tab date-grouping fix is committed and deployed.

## Infrastructure facts (new since 06-11 morning)

- **GitHub `schedule:` cron is NOT the heartbeat anymore** — it throttled to ~9 runs/day on opening
  day. The heartbeat is Cloudflare Worker **`worldcup2026-poll-trigger`** (Andreas's CF account, same
  as Pages): cron `*/15 * * * *`, secret `GH_PAT` (fine-grained, Actions R/W, expires 2026-07-31),
  logs enabled. Canonical code: `workers/poll-trigger/worker.js` + README. Worker dispatches show in
  Actions as "Manually run by abjorck-droid" — distinguish from real button presses by the
  quarter-hour timing. Workflow keeps its own cron as free backup; concurrency group dedupes.
  Verified 23:15/23:30 UTC on 06-11; polling ran clean through both 06-12 matches.
- API-Football paid plan: 7,500 req/day; worst-case knockout day ≈ 900. No quota concern at 15 min.
- `scripts/unlock_player.py <Name>`: clears `locked_at` (PATCH; picks untouched), snapshots rows
  first to gitignored `scripts/snapshots/`. Pre-kickoff emergencies only; both players should bless.

## Fixed this session (all deployed)

1. **Post-lock bonus bets were broken** ("Bonus bets saved ✓ 0"): `onChange` dropped every edit when
   `LOCKED`, including the deliberately-still-enabled bonus tab. Now only `bonus|` keys on un-started
   matches pass the lock gate. 4 regression checks; suite 57/57. Same root cause made the Over/Under 2.5
   value dropdown show its Yes/No fallback — display labels were already Over/Under by design (stored
   values are always Yes/No; engine maps yes = Over).
2. **Bonus tab date grouping**: was keyed on the UTC date slice while labels rendered viewer-local →
   Sat-ET matches under a Fri header. Now groups by `localDayKey()` (viewer-local calendar day).
   Grouping is intentionally viewer-local: each viewer sees headers consistent with the local kickoff
   times shown on the rows.
3. **live.html Scoreboard layout**: Live now (if any) → `.toprow` grid: Points by category (340 px,
   tweak in `.toprow` CSS) beside Latest results (≤4 cards, reflowing) → Up next below. Stacks single
   column ≤880 px. Dashed placeholder if no results yet.
4. **Pundit notes coexist across rows** (clarified, no code change): match-pick note and bonus-bet
   note(s) live on separate label-keyed rows; bonus full-replace can't touch the match row. Notes ride
   their row (clearing a bonus bet pre-kickoff deletes its note) and unseal at that row's resolution.

## Outstanding

1. **Cal heads-up**: any bonus bets he tried to enter *after* his re-lock on 06-11 were silently
   dropped by the bug in (1) — he should re-enter under the fixed page.
2. UX hardening (carried): confirm-dialog when locking an incomplete set ("won't block locking" warnbox
   is too quiet); sticky save-error banner (5 s timeout hid a failed save); server-side lock guard on
   `/api/save` (currently client-only — the unlock tool exploits this deliberately, but so could a bug).
3. Airtable cosmetics (carried from 06-06/06-10): SideGames resolution_type/description, stray "S"
   group option, stale bonus options, `Matches.elapsed`, `PoolPlayers.display_color`, Cal's magic link.
4. **June 26–28**: mulligan usage window (group stage end → R32) — agree mechanics; knockout fixtures
   via `load_fixtures.py`, round names into `ROUND_TIER_OVERRIDES`, `site/bracket_map_2026.json`
   (fixture_id → FIFA match no.); planned "still alive" indicator on the live board.
5. Sanity-check own-goal team attribution on the first real own goal (resolver takes the fixture's
   other side vs API-Football's benefiting-team tag).

## Gotchas for whoever works on this next

- Sandbox has no outbound network: Airtable/API verification runs as local scripts on the Mac.
- No `git add/commit` from the sandbox (index.lock); Andreas commits via GitHub Desktop.
- Fetches of `/api/public` truncate silently around 86 KB — post-lock payloads exceed it. **Parse, don't
  grep**; pick-level checks need a local Airtable script. (Caused a false "your picks are gone" alarm
  on 06-11 — see that handoff's addendum item 4.)
- `/api/public` is edge-cached 60 s and can serve much staler — cache-bust with `?cb=` when verifying.
- The four bracket-template mirrors (index.html, [[route]].js, pickentry_server.py,
  data/thirds_alloc_2026.json) must stay in sync; tests cover all of them.

## What I'd do first next session

Nothing is on fire. Whatever Andreas brings — most likely candidates are the hardening items, mulligan
window mechanics design before June 26, or live-board polish now that real data is flowing.
