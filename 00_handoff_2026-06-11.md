# Session Handoff — 2026-06-11 (match day, URGENT)

**Read this first.** Supersedes `00_handoff_2026-06-10.md`.
Kickoff is **today 15:00 ET / 21:00 CET (MEX–RSA, Azteca)**. This session fixed a **real bracket bug**
Andreas caught while entering picks: the pick-entry tree was propagating knockouts through the made-up
"house template", not the official FIFA bracket. **Commit + deploy + re-check picks + lock before kickoff.**

---

## The bug (reported by Andreas, confirmed)

- Mexico picked 1A and Czechia 3A → the tree paired them in the R32. Impossible: officially **1A plays a
  third from C/E/F/H/I** (match 79), never its own group. 3A can only land on **1E or 1G**.
- England (1L) and France (1I) met in a quarter-final. Impossible: 1I sits in the **M101 half**, 1L in the
  **M102 half** — as group winners they can only meet in the **Final**.

Root cause: `buildR32` in `site/index.html` used an invented pre-draw template ("winners A–H face thirds in
letter order…") and `chainIdx` advanced matches by `ceil(m/2)` adjacency. The live board (`live.html
BR_SRC`) already had the official structure — the two pages disagreed. The same wrong template was mirrored
in the API guard (`functions/api/[[route]].js bracketGuard`) and `scripts/pickentry_server.py bracket_guard`.

## The fix (all mirrors, kept in sync)

1. **Official R32** (FIFA matches 73–88, exact slot order): 73 2A-2B · 74 1E-3rd · 75 1F-2C · 76 1C-2F ·
   77 1I-3rd · 78 2E-2I · 79 1A-3rd · 80 1L-3rd · 81 1D-3rd · 82 1G-3rd · 83 2K-2L · 84 1H-2J ·
   85 1B-3rd · 86 1J-2H · 87 1K-3rd · 88 2D-2G.
2. **Annex C third-place allocation**: FIFA's full 495-row table (every combination of 8 advancing
   third-place groups → which third faces 1A/1B/1D/1E/1G/1I/1K/1L). Canonical copy in
   **`data/thirds_alloc_2026.json`** (new); identical compact blobs embedded in `site/index.html`,
   `functions/api/[[route]].js`, `scripts/pickentry_server.py` — keep all four in sync.
3. **Official feeds** replace the ceil-chain: R16 89=W74/W77, 90=W73/W75, 91=W76/W78, 92=W79/W80,
   93=W83/W84, 94=W81/W82, 95=W86/W88, 96=W85/W87; QF 97=89/90, 98=93/94, 99=91/92, 100=95/96;
   SF 101=97/98, 102=99/100; Final 104. (Note R32→R16 and R16→QF are *not* adjacent-pair.)
4. UI: bracket rows + ghost feeders now show **FIFA match numbers** (M73…M104); copy no longer says
   "house template".

**Verified against** the official FIFA Match Schedule v17 PDF Andreas dropped in the repo
(`FWC26 Match Schedule_v17_10042026_EN.pdf`) — every pairing and feed matches — plus the regulations'
Annex C table (validated programmatically: all 495 rows are permutations of their combination and respect
the per-match group sets).

## Tests — all green

- `test_bracket_logic` **49/49** (was 43) — new: full 495-combo Annex C sweep (no same-group R32 tie,
  32 entrants each), official-feed chainIdx checks, **1I/1L opposite-halves regression**.
- `test_api_save` **22/22**, `test_pickentry_guard` **19/19**, `test_consistency` **13/13**.
- `py_compile` clean; scenario check: 1A never meets 3A in any of the 330 A-containing combinations.

## ⚠ Impact on in-progress picks (both unlocked, neither affected in Airtable yet)

Bracket storage is identity-based sets, so on next page load `pruneAssign` re-derives every slot under the
correct tree: surviving picks (and tokens) move to their proper slots; **pairs of picks that now collide
earlier are voided** and must be re-decided. Expect the completeness counter to drop — the lock gate will
list what's missing. **Both players must re-open pick-entry after deploy, review the knockout tab top to
bottom, fill voided slots, then LOCK.**

## Commit batch for Andreas (GitHub Desktop) — one push, then hard-refresh

- `site/index.html` — official template, Annex C blob, feeds, FIFA match labels
- `functions/api/[[route]].js` — guard rebuilt on official bracket
- `scripts/pickentry_server.py` — same, in Python
- `data/thirds_alloc_2026.json` — canonical Annex C table (new)
- `tests/test_bracket_logic.mjs`, `tests/test_api_save.mjs`, `tests/test_pickentry_guard.py` — official mirrors
- `00_handoff_2026-06-11.md` (this file)

Static assets and the Pages Function deploy atomically on one push, so page and API can't disagree mid-deploy.

## Addendum — Dark Horse eligibility filter (same session)

The Dark Horse dropdown now **hides FIFA top-16 teams** so nobody can accidentally pick an ineligible
side. Eligibility pins to the **10 June 2026 ranking edition** (the one in force at lock; next FIFA
update is 20 July — after the final). Details:

- `data/team_meta_2026.json` updated to the June edition: **Uruguay 16 (DH illegal), USA 17 (DH legal)**
  — the only boundary-relevant move vs April; edition + readme updated.
- Bootstrap now serves `fifa_ranking` (both `[[route]].js` and `pickentry_server.py`).
- `teamOptions(sel, dhOnly)` in `site/index.html`: Dark-Horse mode skips rank ≤ 16, shows each team's
  rank (e.g. "UZB · Uzbekistan (#52)"), and keeps an already-saved ineligible pick visible but flagged
  "INELIGIBLE: top 16" so a stale save is never silently hidden. **Side-bet team dropdowns (First Red
  Card / First Own Goal) are unfiltered** — only `darkhorse` passes the flag.
- Tests: harness teams carry ranks; 4 new checks (hide top-16, show rank-17+, side bets unfiltered,
  stale ineligible pick flagged). Battery: **53/53, 22/22, 19/19, 13/13**, py_compile clean.

⚠ **Ordering matters:** Airtable still holds April ranks (USA 16 / URU 17). Run
`python3 scripts/backfill_team_meta.py` on the Mac **after pulling this commit and before re-opening
pick-entry** — otherwise the filter hides USA and offers Uruguay, exactly backwards. The script is
idempotent and only writes changed fields.

Add to the commit batch: `data/team_meta_2026.json`, `tests/page_harness.mjs` (and the already-listed
`site/index.html`, `functions/api/[[route]].js`, `scripts/pickentry_server.py`, `tests/test_bracket_logic.mjs`).

## Outstanding (carried)

1. Commit/push above → hard-refresh → **both re-check + lock before 15:00 ET**.
2. First-poll-day watch today (poller → resolver → engine; `--dry-run --verbose` locally).
3. Airtable cosmetics carried from June 6/10 (SideGames resolution_type/description, stray "S" group
   option, stale bonus options, `Matches.elapsed`, `PoolPlayers.display_color`, Cal's magic link).
4. June 27–28: knockout fixtures via `load_fixtures.py`, `ROUND_TIER_OVERRIDES`,
   `site/bracket_map_2026.json` (fixture_id → FIFA match no; tree falls back to kickoff order meanwhile).

## What I'd do first next session

Confirm both locked before kickoff and the first poll cycle landed. If anyone's bracket shows surprising
gaps, it's the re-prune doing its job — check the lock-gate missing-slots list, not the engine.

---

## Addendum — match-day evening session (poller fix, unlock tool, false alarm)

1. **Cal's premature lock**: locked before side bets, pre-kickoff. New `scripts/unlock_player.py <Name>`
   clears `locked_at` (PATCH, picks untouched), snapshots all rows to `scripts/snapshots/` (gitignored —
   repo is public, snapshots can hold pre-reveal picks) for before/after diffs. Worked; Cal re-locked in time.
2. **Poller wasn't firing**: GitHub `schedule:` cron is best-effort — ~9 runs/day actual vs 96 expected
   (run #45 fired 17:01 UTC, then nothing through kickoff). All runs that did fire were green; secrets fine.
   Stopgap: local 10-min loop on the Mac (poller → resolver → engine). Permanent: **`workers/poll-trigger/`**
   — Cloudflare Worker cron dispatching `scoreboard-poll.yml` every 15 min via `workflow_dispatch`
   (setup in its README; needs a fine-grained PAT, Actions R/W, expiry 2026-07-31). Workflow keeps its
   cron as backup; concurrency group dedupes.
3. **DH addendum was never pushed**: the morning commit predated it. Tests re-run green (53/22/13/19),
   pushed mid-match (safe: both locked, live.html untouched). `.gitignore` `data/*.json` had also been
   silently swallowing `thirds_alloc_2026.json` + `team_meta_2026.json` — now negated and committed.
   `backfill_team_meta.py` run: Airtable carries June ranks (URU 16/USA 17 → confirmed via API).
4. **False alarm, lesson recorded**: post-lock `/api/public` (~150KB) exceeds the fetch tool's silent
   ~86KB cap; a truncated grep made Andreas's picks look 92% missing. They weren't — Airtable holds the
   full ~270. Rule: parse JSON before reasoning; a payload that should have grown but didn't = truncation.
   Pick-level verification belongs in a local Airtable script, not fetches of `/api/public`.
5. **UX hardening surfaced (real code paths, no harm done today)**: lock proceeds on incomplete sets with
   only a warnbox ("won't block locking") — add a confirm dialog; save errors vanish after 5 s
   (`showErr` timeout) — make failures sticky. Neither urgent while both players are locked.
