# Session Handoff — 2026-07-17

**Read this first.** Supersedes `00_handoff_2026-07-05.md`. Endgame session: verified the pool
is NOT yet mathematically decided, designed + adopted the **v1.2 "Stoppage Time" pot**
(docs 08/09), and built real sealed entry for it through the site. **Files are on disk —
commit via GitHub Desktop.** No Airtable writes from here (no sandbox network).

## State of play (checked live 2026-07-18 00:46Z)
Andreas 1563 · Cal 1414. SFs: ESP 2–0 FRA, ARG 2–1 ENG → Final ESP–ARG (Jul 19 19:00Z),
3rd place FRA–ENG (Jul 18 21:00Z). Andreas has a locked +60 (Total Goals 280 v 187, actual
already 297) → floor 1623. Cal ceiling under pre-v1.2 rules: 1414+150 (ESP champion)+70
(bonus sweeps) = 1634 → an 11-pt live path. Golden Glove dead for both (Unai Simón kept all
6 Spanish clean sheets — record streak; Maignan max 5, Martínez max 3). Golden Boot: Messi
8g/4a v Mbappé 8g/3a — Andreas's Mbappé ×Double (+120) rides on it; FIFA tiebreak
goals→assists→joint (resolve_sidegames.py). Adopting v1.2 ring-fences the last-two-game
betting → main game decided at adoption (Cal ceiling 1564 < 1623), margin M lands 59–329.

## v1.2 in one line
All betting on fixtures **1591865** (3rd place) & **1591866** (Final) writes
`prediction_type="stoppage_bet"` + `pot="stoppage"` — invisible to the scoring engine's
dispatch (falls through, 0 pts), scored manually in `pot_points`, tallied by
`setup_stoppage_pot.py --tally` into `max(1, M−N)` / `M+|N|` recorded margin.

## Changes on disk
- `scripts/setup_stoppage_pot.py` — NEW. Schema migration (fields/options/Duel row/token
  init) + `--tally`. Idempotent, `--dry-run`, stdlib, .env.local pattern.
- `functions/api/[[route]].js` — savebonus accepts `bonus|fid|3` (pot only),
  `stoppage|fid|outcome`, `stoppage|fid|exact`, `stoppage|duel` (anchored to 1591865 for
  lock/reveal); wild types pot-only; Stoppage2x budget 1 (classic tokens rejected on pot
  rows & vice versa); pot rows sealed in /api/public until kickoff exactly like bonus bets;
  players payload gains `stoppage_pot`/`stoppage_token`; picks gain `pot`/`pot_points`.
  save()'s full-replace only deletes bracket types — pot rows safe.
- `site/index.html` — bonus tab: pot fixtures get outcome/exact rows, 3 bet slots,
  wild menu (10 types), the Duel (3rd-place card), Stoppage ×2-only token selects,
  autosave extended to all pot keys; pot rows excluded from the main /api/save collector.
- `tests/test_api_savebonus_stoppage.mjs` — NEW, 27 checks (through the real router).
- Docs: `08_…proposal.md` (ADOPTED status + PDF), `09_stoppage_pot_airtable_migration.md`.

## Test status
**17/17 files green** (9 mjs + 7 py + new file). `node --check` route: OK; inline JS: OK.

## Deploy (on the Mac — ORDER MATTERS, before Jul 18 21:00Z)
1. `python3 scripts/setup_stoppage_pot.py --dry-run` then live — **must run before the
   push**: savebonus upserts don't typecast, so the new select options must exist or every
   pot save 422s. (Endpoint guards give a clear error pointing at the script.)
2. Commit via GitHub Desktop: route.js, index.html, new test, script, docs 08/09/pdf, this
   handoff. Push to `main` → CF Pages deploys functions+site together; edge cache 60 s.
3. Verify: pick-entry link → Bonus tab shows "Stoppage pot" blocks on both cards; save a
   test bet → Airtable row has `stoppage_bet`/`pot=stoppage`; `/api/public` must NOT show
   it pre-kickoff; clear the test bet.
4. Cal signs off on doc 08 (status line says awaiting him).

## Post-final workflow
Manual `pot_points` pass (values in doc 08; ×2 if Stoppage2x; +3 Beat-Rival on outcome rows
where exactly one player was right; two judged wild types are pool-decided) → engine
resolves legacy items → `python3 scripts/setup_stoppage_pot.py --tally` prints the recorded
margin → log it in the 03 spec changelog.

## Live-board pot display (added same session, second push)
`site/live.html`: header shows each player's **Stoppage pot** (accent-coded, "×2 in hand"
tag while the token is unspent); the two pot fixtures' match cards route to a dedicated
`stoppageStrip` — sealed-until-kickoff line, then revealed chips for outcome/exact/Duel/
bets (wild short labels added to `bonusLabel`), marks from the manual `pot_points` pass,
and **no one-sided voiding** (co-participation is waived for the pot). Category totals
still ignore `stoppage_bet` rows by design — the ring-fence is visual too. Pure helpers
(`STOPPAGE_FIXTURES`, `isStoppageFixture`, `stopPicksFor`, `stoppageLabel`) live in the
PURE block; `test_live_alive.mjs` 54 → 68 checks.

## Yes-only wild bets (added same session, third push)
Rare-event wild types are **yes-only** — "No" on a <25% event at flat payout was free
points (worse ×2). Yes-only: Hat-trick, Keeper saves penalty, Own goal, No second-half
goals, Decided on penalties. Kept two-sided: VAR (red-card-grade asymmetry, precedent),
Extra time (2026 regs give the 3rd-place game full ET — verified), 90'+ goal, Sub scores,
5+ cards. Enforced server-side (clear 400), UI renders Yes-only value selects and
auto-fills Yes; a stale saved "No" is dropped from autosave payloads and server-deleted
(self-healing). The YES_ONLY list lives in **three files** (route.js / index.html /
live.html) — `test_api_savebonus_stoppage` has a cross-file drift check. Doc 08 menu
table updated + PDF regenerated. Suites: savebonus 27→35, live_alive 68→69.

## Gotchas (unchanged + new)
No sandbox network; no git from sandbox; edge cache 60 s; `/api/public` truncates ~86KB via
web_fetch (this session read it via Chrome-extension fetch instead — clean). NEW: live.html
ignores `stoppage_bet` rows in category totals by design (ring-fence); the pot renders only
in the header line + per-fixture stoppageStrip. Pot standings show 0 until the manual
`pot_points` pass after each match — that's bookkeeping lag, not a bug.
