# Session Handoff — 2026-06-10

**Read this first.** Supersedes `00_handoff_2026-06-09.md`.
Kickoff is **2026-06-11 — tomorrow.** The June-9 propagated-bracket batch is **committed, pushed,
deployed and confirmed working**; both players are mid-entry through the tree. This session was
design polish + one pre-lock rule change. No engine, lock, or bracket logic touched.

---

## What this session accomplished

1. **Open questions closed (both):**
   - **Custom subdomain — decided against.** Staying on the default Cloudflare `pages.dev` URL.
   - **Dark Horse placement — moved to top of the Brackets tab** (`site/live.html`): the two
     cards now render directly under the "Brackets" heading, above the toolbar and tree/ladders,
     instead of below them.

2. **Pick-entry fixes (`site/index.html`):**
   - **Empty red bar bug fixed.** `#errBanner` (save/load error banner) was permanently visible
     because `.banner.err{display:block}` overrode the base `display:none`. Declaration removed;
     `showErr()` still shows it inline on real errors (5 s auto-hide). Div kept — it's load-bearing.
   - **Player dropdowns got search.** A "Type to search players…" box above each footballer
     select (Golden Boot, Golden Glove, per-group scorers). Accent-insensitive (NFD-normalized),
     matches name or team code, respects the existing gk/group filters, and the current pick
     always stays in the list (prepended) even when the query excludes it. Pure UI — the select,
     `onChange`, and save logic are untouched. Locked state disables the box like everything else.

3. **Flag emojis surfaced** (`Teams.flag_emoji` was already backfilled in Airtable):
   - **API** (`functions/api/[[route]].js`): `flag_emoji` added to the public-view Teams fetch
     and payload (`teams[].flag`).
   - **Live board** (`site/live.html`): flags render in match cards (Scoreboard + Group matches
     tabs, next to the team code), standings tables, bracket-tree slots, the Predicted Champion
     box, and the Dark Horse cards. Eyeballed via `?demo=1` (demo teams carry flags now) —
     judged not too noisy in the tree; keeping everywhere.
   - Everything falls back cleanly when `flag` is missing (no flag, no gap).

4. **Side-bet rule change (pre-lock, agreed 2026-06-10): First Red Card (30) and First Own Goal
   (40) are now TEAM picks**, not player picks — naming the exact player was too hard. Neither
   player had placed these bets, so no migration.
   - **Pick-entry**: the two moved from `SIDE_PLAYER` (squad dropdowns) to a new `SIDE_TEAM`
     list rendered with the existing `teamRow` (same dropdown Dark Horse uses). Save path was
     already generic (`team_id` → `predicted_team` link, both in `[[route]].js` and
     `pickentry_server.py`) — **zero server changes needed**.
   - **Resolver** (`scripts/resolve_sidegames.py`): writes the **team name** as `resolved_value`
     (the engine already matches team picks by name — no engine change). Attribution:
     - *Red card* → the carded player's team (API-Football tags card events with that team).
     - *Own goal* → the team whose player scored it. ⚠ API-Football tags own-goal events with
       the **benefiting** team, so the resolver attributes the fixture's **other** side. Logic
       unit-checked in the sandbox. Sanity-check the first real own goal when it lands — if
       it auto-resolves wrong, fix the value by hand in Airtable; write-once semantics mean the
       resolver won't overwrite a manual value (only `--force` does).
   - **Docs**: `03_Scoring_spec_v1.0.md` amended (points unchanged), `create_base.py` seeds
     updated for posterity, live-board demo data switched to team-style picks.

5. **Verification**: all suites green after every change — `test_bracket_logic` 43/43,
   `test_api_save` 22/22, `test_pickentry_guard` 19/19; `node --check` on both page scripts and
   the API; `py_compile` on all touched scripts; resolver team-attribution unit-checked.

---

## ⚠ Standing rules (unchanged)

- **No git writes from the sandbox** (stale index.lock). Andreas commits via GitHub Desktop.
- Sandbox has no outbound network; anything touching Airtable/API-Football runs on the Mac.

## Commit batch for Andreas (GitHub Desktop) — one push

- `site/index.html` — banner fix, player search, team-pick side bets
- `site/live.html` — Dark Horse placement, flags, demo data
- `functions/api/[[route]].js` — flag_emoji in public payload
- `scripts/resolve_sidegames.py` — team attribution for red card / own goal
- `scripts/create_base.py` — seed parity (cosmetic)
- `03_Scoring_spec_v1.0.md` — side-bet amendment
- `00_handoff_2026-06-10.md` (this file)

After the Pages deploy, **hard-refresh pick-entry** (you need the team dropdowns before placing
those two side bets) and the live board.

## Outstanding — Andreas's actions

1. **Commit & push the batch above**, hard-refresh, place the red-card/own-goal team picks.
2. **Both: finish entering and LOCK picks before kickoff 2026-06-11.**
3. *(Cosmetic, whenever)* In Airtable: flip `SideGames.resolution_type` to `team` and update
   `description` on First Red Card / First Own Goal; plus the carried June-6 cosmetics
   (stray "S" group option, stale `bonus_bet_type` options).
4. Carried if not yet done: `Matches.elapsed` number field, `PoolPlayers.display_color`
   (#219EBC / #FB8500), Cal's magic link.
5. **First-poll-day watch tomorrow June 11** (poller → resolver → engine;
   `--dry-run --verbose` locally for a check).
6. **At draw time (~June 26)**: knockout fixtures via `load_fixtures.py`, fill
   `ROUND_TIER_OVERRIDES`, fill `site/bracket_map_2026.json`. Tree-slot flags become visible
   then (pre-draw the slots are template placeholders).

## Open questions

None. (Subdomain and Dark Horse placement both closed this session.)

## Addendum — session 2 (2026-06-10, picks-entry evening)

**Group-pick consistency warnings added** (`site/index.html`, advisory only — never blocks
save/lock, zero server changes). From the match-outcome picks, each team's group points are
computed as exact or [min,max] (blank match = 0–3). A soft amber warning appears **only when
the called finishing order is mathematically impossible** (lower-placed team guaranteed more
points than a higher-placed team can reach). Equal points = tiebreakers = silent, by design.
Rendered in two places: the group's card on the Brackets tab and under that group's matches
on the Group matches tab; live-updates on every outcome/order change via `updateSummary()`.
New suite `tests/test_consistency.mjs` (13/13); full battery re-run green (43/43, 22/22, 19/19).
**Commit:** `site/index.html`, `tests/test_consistency.mjs`, this file. Hard-refresh after deploy.

## What I'd do first next session

It's match day(s): confirm both locked, then watch the first poll cycle land — poller writes
events/scores, resolver stays quiet until a red card/own goal/group completes, engine scores
the first match picks. Flags and Dark Horse cards should appear on the live board with real
data. Then nothing until the June-26 draw work.
